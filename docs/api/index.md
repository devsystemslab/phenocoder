# API reference

The public entry point is the {class}`~phenocoder.Phenocoder` class, which orchestrates the full
workflow. The remaining modules document the model architectures, data generators and spatial
analysis utilities it builds on.

```{toctree}
:maxdepth: 1

phenocoder
models
generator
spatial
sampling
utils
```
